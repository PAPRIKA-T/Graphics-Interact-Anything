#pragma once
#include <QTabWidget>

/****************************************************标签控件*********************************************************/

class TabWidget : public QTabWidget
{
public:
    TabWidget(QWidget* parent = nullptr);
    ~TabWidget();
};
